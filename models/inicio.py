from odoo import models, fields

class BibliotecaInicio(models.Model):
    _name = 'biblioteca.inicio'
    _description = 'Vista de Inicio para el Módulo de Biblioteca'

    # Aquí puedes agregar campos si lo necesitas, pero para una pantalla estática no son necesarios como es INICIO no hace falta. 
